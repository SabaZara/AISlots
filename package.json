{
  "name": "aislots",
  "version": "4.7.0",
  "private": true,
  "type": "module",
  "description": "AISlots World Forge — one provably fair 99% RTP slot with 1,344 generated visual combinations.",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "test": "node --test",
    "check": "node --check app.js && node --check experience-engine.js && node --check fairness.js && node --check game-model.js && node --check presentation.js",
    "audit:rtp": "node scripts/rtp-simulation.js"
  }
}
