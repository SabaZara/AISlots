{
  "name": "aislots",
  "version": "2.11.0",
  "private": true,
  "type": "module",
  "description": "AISlots — four visually distinct free-play slot games with generated artwork and exact published RTP.",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "test": "node --test",
    "check": "node --check app.js && node --check experience-engine.js && node --check fairness.js && node --check game-model.js && node --check presentation.js",
    "audit:rtp": "node scripts/rtp-simulation.js"
  }
}
